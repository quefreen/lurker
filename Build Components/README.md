
  # Build Components

  This is a code bundle for Build Components. The original project is available at https://www.figma.com/design/1oKutJibDNdYR0pwf2da8s/Build-Components.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  